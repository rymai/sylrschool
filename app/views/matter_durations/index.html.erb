<p id="notice"><%= notice %></p>

<h1>Listing Matter Durations</h1>

<%= datagrid_form_for @grid, :method => :get, :url => matter_durations_path %>


<%= will_paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= will_paginate(@grid.assets) %>

<div class="datagrid-actions">
<%= link_to 'New Matter duration', new_matter_duration_path %>
</div>
