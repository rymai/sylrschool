<h1><%=t(:title_index_teaching)%></h1>
<%= datagrid_form_for @grid, :method => :get, :url => teachings_path %>

<%= will_paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= will_paginate(@grid.assets) %>


<div class="datagrid-actions">
<%= link_to t(:title_new_teaching), new_teaching_path %>
</div>