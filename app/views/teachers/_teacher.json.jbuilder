json.extract! teacher, :id, :firstname, :lastname, :adress, :postalcode, :town, :birthday, :description, :custo, :defmatter_id, :defgradecontext_id, :created_at, :updated_at
json.url teacher_url(teacher, format: :json)
