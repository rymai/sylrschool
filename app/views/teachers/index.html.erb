<p id="notice">
    <%= notice %>
</p>

<h1>Listing Teachers</h1>
<%= datagrid_form_for @grid, :method => :get, :url => teachers_path %>


<%= will_paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= will_paginate(@grid.assets) %>

<div class="datagrid-actions">
<%= link_to 'New Teacher', new_teacher_path %>
</div>
