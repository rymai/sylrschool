<p id="notice"><%= notice %></p>

<h1>Listing Notebooks</h1>

<%= datagrid_form_for @grid, :method => :get, :url => notebooks_path %>


<%= will_paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= will_paginate(@grid.assets) %>

<div class="datagrid-actions">
<%= link_to 'New Notebook', new_notebook_path %>
</div>
