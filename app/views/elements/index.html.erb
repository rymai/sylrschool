<p id="notice"><%= notice %></p>

<h1>Listing Elements</h1>

<%= datagrid_form_for @grid, :method => :get, :url => elements_path %>


<%= will_paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= will_paginate(@grid.assets) %>

<div class="datagrid-actions">
<%= link_to 'New Element', new_element_path %>
</div>
