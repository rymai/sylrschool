<%= datagrid_form_for @grid, :method => :get, :url => class_schools_path %>


<%= will_paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= will_paginate(@grid.assets) %>

<div class="datagrid-actions">
<%= link_to 'New Class school', new_class_school_path %>
</div>
